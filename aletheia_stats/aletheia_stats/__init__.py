# Aletheia-Stats Module
# Main application package for Aletheia-Stats.

# This __init__.py file can be used to:
# 1. Mark the directory as a Python package.
# 2. Define package-level attributes or import widely used submodules.
# 3. Perform package-level initialization if necessary.

# For now, it primarily serves to make the `aletheia_stats` directory
# importable as a package.

VERSION = "0.1.0"

# Optionally, you could make key components easily accessible, e.g.:
# from .domain.services import StatsService
# from .application.use_cases import PerformTTestUseCase

# However, it's often cleaner to let users import directly from the submodules
# to maintain clarity about where components originate.

# print(f"Aletheia-Stats package (version {VERSION}) initialized.")
