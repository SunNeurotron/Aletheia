# Presentation Package for Aletheia-Stats
# This package is responsible for handling incoming requests (e.g., HTTP API)
# and presenting responses to the user/client. It interacts with the
# application layer (use cases) to fulfill requests.

# __init__.py for the presentation package.
# You might make the main API router or app instance accessible if needed,
# but typically, the application is assembled in `main.py`.

# from .api import api_router

# For now, it serves to mark the directory as a package.
